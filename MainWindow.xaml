<Window x:Class="RssGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RSS Generator Utility - Premium Edition" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryBrush}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SecondaryBrush}" Padding="20">
            <StackPanel>
                <TextBlock Text="RSS GENERATOR" FontWeight="Black" FontSize="18" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,5"/>
                <TextBlock Text="PREMIUM EDITION" FontSize="10" Foreground="{StaticResource DimTextBrush}" Margin="0,0,0,40"/>

                <Button Content="Dashboard" Height="40" Margin="0,5" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10,0"/>
                <Button Content="Read Articles" Click="OnReadAllArticlesClick" Height="40" Margin="0,5" Background="Transparent" Foreground="{StaticResource DimTextBrush}" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10,0"/>
                <Button Content="Settings" Height="40" Margin="0,5" Background="Transparent" Foreground="{StaticResource DimTextBrush}" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10,0"/>
                <Button Content="Help &amp; Docs" Height="40" Margin="0,5" Background="Transparent" Foreground="{StaticResource DimTextBrush}" BorderThickness="0" HorizontalContentAlignment="Left" Padding="10,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch">
                <TextBlock Text="My Feed Targets" Style="{StaticResource HeadingStyle}" VerticalAlignment="Center"/>
                <Button Content="+ Add New Site" Click="OnAddSiteClick" Margin="20,0,0,0" Padding="15,8" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Quick Stats -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20">
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="15" Margin="0,0,15,0" Width="180">
                    <StackPanel>
                        <TextBlock Text="Active Targets" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                        <TextBlock x:Name="TargetCountText" Text="--" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource SecondaryBrush}" CornerRadius="8" Padding="15" Width="180">
                    <StackPanel>
                        <TextBlock Text="Latest Updates" Foreground="{StaticResource DimTextBrush}" FontSize="12"/>
                        <TextBlock x:Name="UpdateCountText" Text="--" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                
                <Button Content="Run All Crawlers" Click="OnRunAllClick" Margin="15,0,0,0" Padding="20,0" Background="#10B981" Foreground="White" BorderThickness="0" VerticalAlignment="Stretch">
                     <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Targets List -->
            <ListView Grid.Row="2" x:Name="TargetsList" Background="Transparent" BorderThickness="0" Margin="0,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                        <Setter Property="Padding" Value="15"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Style.Resources>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Url}" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="View" Click="OnViewTargetArticlesClick" Tag="{Binding}" Margin="5,0" Padding="10,4" Background="#0ea5e9" Foreground="White" BorderThickness="0"/>
                                <Button Content="Run" Click="OnRunTargetClick" Tag="{Binding}" Margin="5,0" Padding="10,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                                <Button Content="Edit" Click="OnEditClick" Tag="{Binding}" Margin="5,0" Padding="10,4" Background="#334155" Foreground="White" BorderThickness="0"/>
                                <Button Content="Del" Click="OnDeleteClick" Tag="{Binding}" Margin="5,0" Padding="10,4" Background="#7F1D1D" Foreground="White" BorderThickness="0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Log Area -->
            <Border Grid.Row="3" Background="#0A0F1D" CornerRadius="8" Padding="10" Height="100">
                <ScrollViewer x:Name="LogScrollViewer">
                    <TextBlock x:Name="LogText" Text="Application Started." Foreground="#10B981" FontSize="11" TextWrapping="Wrap" FontFamily="Consolas"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
