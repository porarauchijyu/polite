<Window x:Class="RssGenerator.ArticleListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Latest Articles" Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PrimaryBrush}">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Latest Articles" Style="{StaticResource HeadingStyle}" Margin="0,0,0,20"/>

        <!-- Article List -->
        <ListView Grid.Row="1" x:Name="ArticleListView" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                    <Setter Property="Margin" Value="0,0,0,12"/>
                    <Setter Property="Padding" Value="15"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Style.Resources>
                        <Style TargetType="Border">
                             <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                             <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid MouseLeftButtonUp="OnArticleClick">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <DockPanel Grid.Row="0">
                            <TextBlock Text="{Binding SourceTitle}" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="Bold" DockPanel.Dock="Left"/>
                            <TextBlock Text="{Binding PublishedDate, StringFormat='{} {0:MM/dd HH:mm}'}" Foreground="{StaticResource DimTextBrush}" FontSize="11" HorizontalAlignment="Right"/>
                        </DockPanel>
                        
                        <TextBlock Grid.Row="1" Text="{Binding Title}" Foreground="White" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,5"/>
                        
                        <TextBlock Grid.Row="2" Text="{Binding Description}" Foreground="{StaticResource DimTextBrush}" FontSize="13" TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Footer -->
        <Button Grid.Row="2" Content="Close" Click="OnCloseClick" Width="100" Padding="0,10" HorizontalAlignment="Right" Margin="0,15,0,0" Background="#334155" Foreground="White" BorderThickness="0"/>
    </Grid>
</Window>
